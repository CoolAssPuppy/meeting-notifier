# Snapfile for MeetingNotifier (macOS App)
# This file contains the configuration for snapshot
# Use `fastlane snapshot` to generate screenshots

# NOTE: snapshot is primarily designed for iOS
# For macOS apps, we need to use a different approach

# This Snapfile is here for reference but won't work directly
# Instead, use the UI tests directly in Xcode or with xcodebuild

# For macOS screenshot generation, see TESTING_GUIDE.md
