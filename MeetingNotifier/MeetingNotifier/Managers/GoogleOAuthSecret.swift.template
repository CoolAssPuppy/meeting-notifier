import Foundation

struct GoogleOAuthSecret {
    // SETUP INSTRUCTIONS:
    // 1. Copy this file and rename it to: GoogleOAuthSecret.swift
    // 2. Replace YOUR_CLIENT_SECRET_HERE with your actual Google OAuth client secret
    // 3. Get your secret from: https://console.cloud.google.com/apis/credentials
    // 4. GoogleOAuthSecret.swift is gitignored and won't be committed

    static let secret = "YOUR_CLIENT_SECRET_HERE"
}
